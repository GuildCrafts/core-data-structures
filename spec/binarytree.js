import chai, { expect } from 'chai'
import chaiChange from 'chai-change'
import PriorityQueue from '../src/binarytree'

chai.use(chaiChange)

describe('binarySearchTree', () => {
  'use strict'

  it('exists', () => {
    expect(binarySearchTree).to.be.a('function')

  })
})
