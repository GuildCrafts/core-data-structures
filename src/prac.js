front() {
  if(this.size === 0) return null
  let priority = this.element[this.size].priorityInt
  let element = this.elements[0]
    for (let j = 1; j < this.size; j++) {
      if(priority > this.elements[j.priorityInt]) {
        priority = this.elements[j].priorityInt
        element = this.elements[j]
      }
    }
  return element.data
}


font() {
  if(this.size === 0) return null
  let j = this.size
  let priority = this.element[j]
  for (let i = 0; 0 < this.element[j], j--) {
    for (j )
  }

}
